node_modules
.DS_Store
dist
.env
.env.*
!.env.example
.vite
*.log

# Comparação de masters: vídeos pesados e frames extraídos ficam fora do Git.
# Os relatórios (JSON/CSV/MD) são versionados de propósito.
media-comparison/higgsfield-originals/
media-comparison/current-masters/
media-comparison/frames/
media-comparison/**/*.mp4
media-comparison/**/*.mov
media-comparison/**/*.webm
media-comparison/**/*.png
media-comparison/**/*.jpg

# Referências do /quality-diagnostic: geradas por
# media-comparison/scene01-fidelity/tools/make-references.sh, ~27 MB.
# Ficam fora de public/ de propósito para não entrarem no bundle de produção.
diagnostic-refs/
